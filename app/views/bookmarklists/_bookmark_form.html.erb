<%= form_with(model: @bookmarklist, local: true) do |f| %>
    <div class="field">
        <%= f.text_area :name, placeholder: "Create New Bookmark..!!!" %>
    </div>
    <%= f.submit "Create", class: "btn btn-success btn-sm width-100px" %>
<% end %>