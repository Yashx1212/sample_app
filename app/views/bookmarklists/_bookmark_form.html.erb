<%= form_with(model: @bookmarklist, local: true) do |f| %>
    <div class="field">
        <%= f.text_area :name, placeholder: "Create new list" %>
    </div>
    <%= f.submit "Create", class: "btn btn-primary" %>
<% end %>