<% provide(:title, 'All users') %>
<h1>All users</h1>
<%= will_paginate %>
<ul class="users">
    <% @users.each do |user|%>
        <li class="show-users">
            <%= render partial: 'user' , locals: {user: user} %>
        </li>
    <% end %>
</ul>