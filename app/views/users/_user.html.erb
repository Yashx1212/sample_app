
<%= gravatar_for user, size: 50 %>
<%= link_to user.name, user %>
<% if current_user.admin? && !current_user?(user) %>
     <%= link_to user, method: :delete, class: "text-danger " ,data: { confirm: "You sure?" }  do%>
            <i class="bi bi-trash float-r mr-05 "></i>
        <% end %>
<% end %>